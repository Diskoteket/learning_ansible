---

- hosts: all
  become: true
  tasks:

  - name: apt remove apache2 and php packages
    apt:
      name: 
        - apache2
        - libapache2-mod-php
      state: absent
      update_cache: yes
    when: ansible_distribution in ["Debian", "Ubuntu"]

  - name: dnf remove httpd and php packages
    dnf:
      name: 
        - httpd
        - php
      state: absent
      update_cache: yes
    when: ansible_distribution in ["RedHat", "CentOS"]
